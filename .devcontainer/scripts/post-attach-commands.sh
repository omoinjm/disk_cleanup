# Run safewipe list to show available devices on attach
# sudo /workspaces/disk_cleanup/.venv/bin/safewipe list

# Other useful commands:
# 1. Full guided workflow (recommended for first-time users)
# sudo /workspaces/disk_cleanup/.venv/bin/safewipe auto

# 2. Wipe a specific device
# sudo /workspaces/disk_cleanup/.venv/bin/safewipe wipe /dev/sdb

# 3. Wipe with a specific method and verify
# sudo /workspaces/disk_cleanup/.venv/bin/safewipe wipe /dev/sdb --method secure --verify
